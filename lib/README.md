# lib

This directory contains the main Dart source code for the ContactCallExporter Flutter application.

## Files

- `main.dart`: Contains the main application entry point, the UI home page (`ExportHomePage`), export logic for contacts and call history to JSON files, and import logic to restore contacts from JSON files into the device's native address book.
